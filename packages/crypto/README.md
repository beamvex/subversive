# subversive-crypto

This crate contains cryptographic primitives and address management for the Subversive project.

## Modules
- `address`: Key generation, public/private key handling, and address encoding.

## Usage
Add this crate as a dependency in your workspace and use `subversive_crypto::address` for address utilities.
